<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ms.petopia.api.refund.mapper.RefundMapper">

    <select id="selectById" resultType="RefundRow">
        SELECT refund_id
             , payment_id
             , refund_reason
             , requested_by_domain
             , refund_amount
             , status
             , requested_at
             , processed_at
             , created_at
             , updated_at
          FROM refund
         WHERE refund_id = #{refundId}
    </select>

    <select id="selectByPaymentId" resultType="RefundRow">
        SELECT refund_id
             , payment_id
             , refund_reason
             , requested_by_domain
             , refund_amount
             , status
             , requested_at
             , processed_at
             , created_at
             , updated_at
          FROM refund
         WHERE payment_id = #{paymentId}
    </select>

    <insert id="insert" parameterType="RefundRow"
            useGeneratedKeys="true" keyProperty="refundId">
        INSERT INTO refund (
            payment_id, refund_reason, requested_by_domain, refund_amount, status,
            requested_at, processed_at, created_at, updated_at
        ) VALUES (
            #{paymentId}, #{refundReason}, #{requestedByDomain}, #{refundAmount}, #{status},
            #{requestedAt}, #{processedAt}, #{createdAt}, #{updatedAt}
        )
    </insert>

</mapper>

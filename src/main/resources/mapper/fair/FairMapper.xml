<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ms.petopia.api.fair.mapper.FairMapper">

    <!--
        status/reservation_fee는 DDL에 DEFAULT가 걸려 있어(status DEFAULT 'RECEIVED',
        reservation_fee DEFAULT 0) Java 쪽에서 값을 안 채워도 되게 <if>로 뺐다.
        applicant_user_id/name/manager_name/manager_email/created_at/updated_at은
        DDL상 NOT NULL이고 기본값도 없어 서비스 레이어가 항상 채워 넣는다는 전제로 고정 바인딩한다.
        reject_reason/reviewed_by/reviewed_at/published_at/canceled_at은 신청 시점에는
        의미가 없는 값이라 insert 대상에서 아예 뺐다(update에서만 채운다).
    -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="fairId" keyColumn="fair_id">
        INSERT INTO fairs
        <trim prefix="(" suffix=")" suffixOverrides=",">
            applicant_user_id, name, manager_name, manager_email, created_at, updated_at,
            <if test="description != null">description,</if>
            <if test="category != null">category,</if>
            <if test="posterImageUrl != null">poster_image_url,</if>
            <if test="noticeText != null">notice_text,</if>
            <if test="placeName != null">place_name,</if>
            <if test="address != null">address,</if>
            <if test="indoorOutdoor != null">indoor_outdoor,</if>
            <if test="vendorRecruitStartDate != null">vendor_recruit_start_date,</if>
            <if test="vendorRecruitEndDate != null">vendor_recruit_end_date,</if>
            <if test="reservationStartDate != null">reservation_start_date,</if>
            <if test="reservationEndDate != null">reservation_end_date,</if>
            <if test="operationStartDate != null">operation_start_date,</if>
            <if test="operationEndDate != null">operation_end_date,</if>
            <if test="reservationFee != null">reservation_fee,</if>
            <if test="reservationCancelDeadlineHours != null">reservation_cancel_deadline_hours,</if>
            <if test="reservationChangeDeadlineHours != null">reservation_change_deadline_hours,</if>
            <if test="managerPhone != null">manager_phone,</if>
            <if test="status != null">status,</if>
            <if test="paymentDueAt != null">payment_due_at,</if>
            <if test="publicScheduledAt != null">public_scheduled_at,</if>
            <if test="submittedSnapshot != null">submitted_snapshot,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            #{applicantUserId}, #{name}, #{managerName}, #{managerEmail}, #{createdAt}, #{updatedAt},
            <if test="description != null">#{description},</if>
            <if test="category != null">#{category},</if>
            <if test="posterImageUrl != null">#{posterImageUrl},</if>
            <if test="noticeText != null">#{noticeText},</if>
            <if test="placeName != null">#{placeName},</if>
            <if test="address != null">#{address},</if>
            <if test="indoorOutdoor != null">#{indoorOutdoor},</if>
            <if test="vendorRecruitStartDate != null">#{vendorRecruitStartDate},</if>
            <if test="vendorRecruitEndDate != null">#{vendorRecruitEndDate},</if>
            <if test="reservationStartDate != null">#{reservationStartDate},</if>
            <if test="reservationEndDate != null">#{reservationEndDate},</if>
            <if test="operationStartDate != null">#{operationStartDate},</if>
            <if test="operationEndDate != null">#{operationEndDate},</if>
            <if test="reservationFee != null">#{reservationFee},</if>
            <if test="reservationCancelDeadlineHours != null">#{reservationCancelDeadlineHours},</if>
            <if test="reservationChangeDeadlineHours != null">#{reservationChangeDeadlineHours},</if>
            <if test="managerPhone != null">#{managerPhone},</if>
            <if test="status != null">#{status},</if>
            <if test="paymentDueAt != null">#{paymentDueAt},</if>
            <if test="publicScheduledAt != null">#{publicScheduledAt},</if>
            <if test="submittedSnapshot != null">#{submittedSnapshot},</if>
        </trim>
    </insert>

    <select id="selectById" resultType="Fair">
        SELECT fair_id, applicant_user_id, name, description, category, poster_image_url, notice_text,
               place_name, address, indoor_outdoor,
               vendor_recruit_start_date, vendor_recruit_end_date,
               reservation_start_date, reservation_end_date,
               operation_start_date, operation_end_date,
               reservation_fee, reservation_cancel_deadline_hours, reservation_change_deadline_hours,
               manager_name, manager_phone, manager_email,
               status, reject_reason, reviewed_by, reviewed_at,
               payment_due_at, public_scheduled_at, published_at, canceled_at,
               submitted_snapshot, created_at, updated_at
          FROM fairs
         WHERE fair_id = #{fairId}
    </select>

    <!-- 마이페이지 "내 신청 현황" 목록용. 최신 신청이 위로 오도록 created_at 역순 정렬한다. -->
    <select id="selectByApplicantUserId" resultType="Fair">
        SELECT fair_id, applicant_user_id, name, description, category, poster_image_url, notice_text,
               place_name, address, indoor_outdoor,
               vendor_recruit_start_date, vendor_recruit_end_date,
               reservation_start_date, reservation_end_date,
               operation_start_date, operation_end_date,
               reservation_fee, reservation_cancel_deadline_hours, reservation_change_deadline_hours,
               manager_name, manager_phone, manager_email,
               status, reject_reason, reviewed_by, reviewed_at,
               payment_due_at, public_scheduled_at, published_at, canceled_at,
               submitted_snapshot, created_at, updated_at
          FROM fairs
         WHERE applicant_user_id = #{applicantUserId}
         ORDER BY created_at DESC
    </select>

    <!--
        null이 아닌 필드만 갱신한다(승인/반려/재제출마다 채우는 필드가 달라서 범용으로 둠).
        updated_at은 호출부 값과 무관하게 항상 NOW()로 덮어쓴다.
        applicant_user_id, created_at은 갱신 대상에서 제외한다.
    -->
    <update id="update">
        UPDATE fairs
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="category != null">category = #{category},</if>
            <if test="posterImageUrl != null">poster_image_url = #{posterImageUrl},</if>
            <if test="noticeText != null">notice_text = #{noticeText},</if>
            <if test="placeName != null">place_name = #{placeName},</if>
            <if test="address != null">address = #{address},</if>
            <if test="indoorOutdoor != null">indoor_outdoor = #{indoorOutdoor},</if>
            <if test="vendorRecruitStartDate != null">vendor_recruit_start_date = #{vendorRecruitStartDate},</if>
            <if test="vendorRecruitEndDate != null">vendor_recruit_end_date = #{vendorRecruitEndDate},</if>
            <if test="reservationStartDate != null">reservation_start_date = #{reservationStartDate},</if>
            <if test="reservationEndDate != null">reservation_end_date = #{reservationEndDate},</if>
            <if test="operationStartDate != null">operation_start_date = #{operationStartDate},</if>
            <if test="operationEndDate != null">operation_end_date = #{operationEndDate},</if>
            <if test="reservationFee != null">reservation_fee = #{reservationFee},</if>
            <if test="reservationCancelDeadlineHours != null">reservation_cancel_deadline_hours = #{reservationCancelDeadlineHours},</if>
            <if test="reservationChangeDeadlineHours != null">reservation_change_deadline_hours = #{reservationChangeDeadlineHours},</if>
            <if test="managerName != null">manager_name = #{managerName},</if>
            <if test="managerPhone != null">manager_phone = #{managerPhone},</if>
            <if test="managerEmail != null">manager_email = #{managerEmail},</if>
            <if test="status != null">status = #{status},</if>
            <if test="rejectReason != null">reject_reason = #{rejectReason},</if>
            <if test="reviewedBy != null">reviewed_by = #{reviewedBy},</if>
            <if test="reviewedAt != null">reviewed_at = #{reviewedAt},</if>
            <if test="paymentDueAt != null">payment_due_at = #{paymentDueAt},</if>
            <if test="publicScheduledAt != null">public_scheduled_at = #{publicScheduledAt},</if>
            <if test="publishedAt != null">published_at = #{publishedAt},</if>
            <if test="canceledAt != null">canceled_at = #{canceledAt},</if>
            <if test="submittedSnapshot != null">submitted_snapshot = #{submittedSnapshot},</if>
            updated_at = NOW(),
        </set>
        WHERE fair_id = #{fairId}
    </update>

    <!--
        status = 'RECEIVED' 조건을 WHERE에 함께 걸어 조건부 갱신으로 만든다(동시성 방어,
        FairCancelRequestMapper#update와 동일한 패턴). "SELECT로 RECEIVED 확인 후 UPDATE"는
        두 검토 요청이 동시에 들어오면 둘 다 체크를 통과해버릴 수 있는데, 이 조건이 있으면
        먼저 커밋되는 쪽만 실제로 행을 바꾸고 나머지는 영향 행 0건으로 실패한다.
    -->
    <update id="updateReviewResult">
        UPDATE fairs
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="rejectReason != null">reject_reason = #{rejectReason},</if>
            <if test="reviewedBy != null">reviewed_by = #{reviewedBy},</if>
            <if test="reviewedAt != null">reviewed_at = #{reviewedAt},</if>
            <if test="paymentDueAt != null">payment_due_at = #{paymentDueAt},</if>
            updated_at = NOW(),
        </set>
        WHERE fair_id = #{fairId}
          AND status = 'RECEIVED'
    </update>

    <!--
        신청 내용 필드는 null이 아닌 것만 갱신하는 PATCH 방식이지만, status/reject_reason/
        reviewed_by/reviewed_at은 이 UPDATE가 실제로 행을 바꿀 때마다 항상 고정값(RECEIVED/
        NULL/NULL/NULL)으로 되돌린다 - REJECTED에서 수정으로 재제출되면 이전 반려 이력이
        더 이상 유효하지 않기 때문이다. WHERE의 status IN (...) 조건이 동시성 방어를 겸한다
        (updateReviewResult와 동일한 이유 - "SELECT로 확인 후 UPDATE"는 그 사이 다른 요청이
        상태를 바꿔버리는 경합을 못 막는다).
    -->
    <update id="updateApplication">
        UPDATE fairs
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="category != null">category = #{category},</if>
            <if test="posterImageUrl != null">poster_image_url = #{posterImageUrl},</if>
            <if test="noticeText != null">notice_text = #{noticeText},</if>
            <if test="placeName != null">place_name = #{placeName},</if>
            <if test="address != null">address = #{address},</if>
            <if test="indoorOutdoor != null">indoor_outdoor = #{indoorOutdoor},</if>
            <if test="vendorRecruitStartDate != null">vendor_recruit_start_date = #{vendorRecruitStartDate},</if>
            <if test="vendorRecruitEndDate != null">vendor_recruit_end_date = #{vendorRecruitEndDate},</if>
            <if test="reservationStartDate != null">reservation_start_date = #{reservationStartDate},</if>
            <if test="reservationEndDate != null">reservation_end_date = #{reservationEndDate},</if>
            <if test="operationStartDate != null">operation_start_date = #{operationStartDate},</if>
            <if test="operationEndDate != null">operation_end_date = #{operationEndDate},</if>
            <if test="reservationFee != null">reservation_fee = #{reservationFee},</if>
            <if test="reservationCancelDeadlineHours != null">reservation_cancel_deadline_hours = #{reservationCancelDeadlineHours},</if>
            <if test="reservationChangeDeadlineHours != null">reservation_change_deadline_hours = #{reservationChangeDeadlineHours},</if>
            <if test="managerName != null">manager_name = #{managerName},</if>
            <if test="managerPhone != null">manager_phone = #{managerPhone},</if>
            <if test="managerEmail != null">manager_email = #{managerEmail},</if>
            status = 'RECEIVED',
            reject_reason = NULL,
            reviewed_by = NULL,
            reviewed_at = NULL,
            updated_at = NOW(),
        </set>
        WHERE fair_id = #{fairId}
          AND status IN ('RECEIVED', 'REJECTED')
    </update>

    <!--
        canceled_at IS NOT NULL만 걸면 취소된 행사 수가 limit를 넘는 순간
        canceled_at DESC 정렬로 매번 같은 최신 N개만 뽑혀서, 그보다 오래된 행사의 PENDING
        결제가 영영 처리되지 못하고 배치 슬롯을 못 받을 수 있다. 그래서 이 결제가 아직 남아있는
        행사만(EXISTS) 고르고 오래된 순(ASC)으로 처리한다 - 한 행사가 다 정리되면(더 이상
        PENDING이 없으면) 이 목록에서 자연히 빠지니 다음 실행에서 그 자리를 다른 행사가 채운다.
    -->
    <select id="selectCanceledFairIds" resultType="long">
        SELECT f.fair_id
          FROM fairs f
         WHERE f.canceled_at IS NOT NULL
           AND EXISTS (
               SELECT 1
                 FROM payment p
                WHERE p.fair_id = f.fair_id
                  AND p.status = 'PENDING'
                  AND p.payment_type IN ('RESERVATION_DEPOSIT', 'VENDOR_FEE')
           )
         ORDER BY f.canceled_at
         LIMIT #{limit}
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ms.petopia.api.fair.mapper.FairDateMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="fairDateId" keyColumn="fair_date_id">
        INSERT INTO fair_dates
            (fair_id, operation_date, capacity, entry_start_time, entry_end_time, created_at, updated_at)
        VALUES
            (#{fairId}, #{operationDate}, #{capacity}, #{entryStartTime}, #{entryEndTime}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="selectById" resultType="FairDate">
        SELECT fair_date_id, fair_id, operation_date, capacity, entry_start_time, entry_end_time, created_at, updated_at
          FROM fair_dates
         WHERE fair_date_id = #{fairDateId}
    </select>

    <select id="selectByFairIdAndDate" resultType="FairDate">
        SELECT fair_date_id, fair_id, operation_date, capacity, entry_start_time, entry_end_time, created_at, updated_at
          FROM fair_dates
         WHERE fair_id = #{fairId}
           AND operation_date = #{operationDate}
    </select>

    <!--
        reservedCount: 이 운영일의 정원을 점유 중인 사전예약(ADVANCE) 건수.
        예전에는 reservations를 직접 세는 서브쿼리였으나, 지금은 예약 도메인이 정원 판정에
        쓰는 fair_dates.reserved_count를 그대로 읽는다. 같은 값을 두 방식으로 계산하면
        어긋났을 때 어느 쪽이 맞는지 알 수 없어서, 판정에 쓰는 값 하나만 본다.
        onsiteSalesConfigured: 이 운영일에 현장예매 정책이 이미 설정돼 있는지.
        둘 다 관리자 화면이 정원 축소·삭제 전 경고를 보여주는 용도일 뿐, 여기서 직접 막지 않는다.
    -->
    <sql id="statsColumns">
        SELECT fd.fair_date_id
             , fd.fair_id
             , fd.operation_date
             , fd.capacity
             , fd.entry_start_time
             , fd.entry_end_time
             , fd.created_at
             , fd.updated_at
             , fd.reserved_count
             , EXISTS (SELECT 1
                         FROM onsite_sales_policies osp
                        WHERE osp.fair_date_id = fd.fair_date_id) AS onsite_sales_configured
          FROM fair_dates fd
    </sql>

    <select id="selectByIdWithStats" resultType="FairDateWithStats">
        <include refid="statsColumns" />
        WHERE fd.fair_date_id = #{fairDateId}
    </select>

    <select id="selectByFairIdWithStats" resultType="FairDateWithStats">
        <include refid="statsColumns" />
        WHERE fd.fair_id = #{fairId}
        ORDER BY fd.operation_date ASC
    </select>

    <!--
        reserved_count <= #{capacity} 조건: FairDateService#update가 미리 SELECT로 확인한
        이후에도 그 사이 새 사전예약이 들어와 reserved_count가 올라갈 수 있다. 이 조건이 없으면
        그 레이스 상황에서 정원이 예약 건수 아래로 내려간 채 UPDATE가 그대로 성공해버린다.
        영향받은 행이 0건이면 이 레이스에 걸린 것이니 서비스 계층에서 다시 거부한다.
    -->
    <update id="update">
        UPDATE fair_dates
           SET capacity = #{capacity},
               entry_start_time = #{entryStartTime},
               entry_end_time = #{entryEndTime},
               updated_at = #{updatedAt}
         WHERE fair_date_id = #{fairDateId}
           AND reserved_count <= #{capacity}
    </update>

    <!-- reserved_count = 0 조건: update와 동일한 이유로, SELECT와 DELETE 사이에 새 예약이
         들어오는 레이스를 막는다. -->
    <delete id="deleteById">
        DELETE FROM fair_dates WHERE fair_date_id = #{fairDateId} AND reserved_count = 0
    </delete>

</mapper>

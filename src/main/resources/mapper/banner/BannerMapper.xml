<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ms.petopia.api.banner.mapper.BannerMapper">
    <!--현재 노출 중인 배너 목록(is_active=1, 기간 유효, sort_order 오름차순)-->
    <select id="selectActiveList" resultType="com.ms.petopia.api.banner.domain.Banner">
        SELECT banner_id, title, image_key, link_url, link_target,
            sort_order, is_active, started_at, ended_at, created_at
        FROM banner
        WHERE is_active = 1
            AND (started_at IS NULL OR started_at &lt;= NOW())
            AND (ended_at IS NULL OR ended_at &gt;= NOW())
        ORDER BY sort_order
    </select>

    <!--전체 배너 목록(관리자)-->
    <select id="selectAll" resultType="com.ms.petopia.api.banner.domain.Banner">
        SELECT banner_id, title, image_key, link_url, link_target,
            sort_order, is_active, started_at, ended_at, created_at
        FROM banner
        ORDER BY sort_order
    </select>

    <!--단건 조회-->
    <select id="selectById" resultType="com.ms.petopia.api.banner.domain.Banner">
        SELECT banner_id, title, image_key, link_url, link_target,
            sort_order, is_active, started_at, ended_at, created_by, created_at, updated_at
        FROM banner
        WHERE banner_id = #{bannerId}
    </select>

    <!--등록-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="bannerId">
        INSERT INTO banner(title, image_key, link_url, link_target, sort_order, is_active,
            started_at, ended_at, created_by)
        VALUES( #{title}, #{imageKey}, #{linkUrl}, #{linkTarget}, #{sortOrder},
            #{isActive}, #{startedAt}, #{endedAt}, #{createdBy}
        )
    </insert>

    <!--수정(null 아닌 필드만 갱신)-->
    <update id="update">
        UPDATE banner
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="imageKey != null">image_key = #{imageKey},</if>
            <if test="linkUrl != null">link_url = #{linkUrl},</if>
            <if test="linkTarget != null">link_target = #{linkTarget},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="startedAt != null">started_at = #{startedAt},</if>
            <if test="endedAt != null">ended_at = #{endedAt},</if>
        </set>
        WHERE banner_id = #{bannerId}
    </update>

    <!--삭제-->
    <delete id="delete">
        DELETE FROM banner WHERE banner_id = #{bannerId}
    </delete>

    <!--노출 여부 토글-->
    <update id="updateActive">
        UPDATE banner SET is_active = #{isActive} WHERE banner_id = #{bannerId}
    </update>

    <!--순서 변경-->
    <update id="updateOrder">
        UPDATE banner SET sort_order = #{sortOrder} WHERE banner_id = #{bannerId}
    </update>
</mapper>
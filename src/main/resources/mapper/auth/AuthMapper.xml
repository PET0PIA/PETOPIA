<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ms.petopia.api.auth.mapper.AuthMapper">

    <!-- ===== 회원가입 / 유저 조회·수정 ===== -->

    <select id="existsVerifiedByEmail" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
              FROM users
             WHERE email = #{email}
               AND email_verified = TRUE
        )
    </select>

    <insert id="insertUser"
            parameterType="User"
            useGeneratedKeys="true"
            keyProperty="userId"
            keyColumn="user_id">
        INSERT INTO users (
               email
             , password_hash
             , nickname
             , birth_date
             , phone
             , gender
             , address
             , agreed_terms
             , agreed_privacy
             , role
             , status
             , email_verified
             , created_at
        ) VALUES (
               #{email}
             , #{passwordHash}
             , #{nickname}
             , #{birthDate}
             , #{phone}
             , #{gender}
             , #{address}
             , #{agreedTerms}
             , #{agreedPrivacy}
             , #{role}
             , #{status}
             , #{emailVerified}
             , #{createdAt}
        )
    </insert>

    <!--
      email_verified = FALSE 만으로는 "이메일 인증 도중 이탈한 미완성 가입 row"를 구분 못 한다.
      OAuth로 만든 유저(비밀번호 없음)도 이제 emailVerified가 provider 값을 그대로 따라가서
      false일 수 있기 때문 - 네이버로 가입한 진짜 유저 row가 이 WHERE 조건에 걸려서,
      같은 이메일로 일반 가입을 시도하는 다른 사람의 값으로 통째로 덮어써질 수 있다(계정 탈취/데이터 손상).
      password_hash IS NOT NULL을 추가로 요구해서, "이 row가 애초에 이메일/비밀번호 가입으로
      만들어졌는가"까지 같이 확인한다 - OAuth 유저는 passwordHash가 항상 null이라
      이 조건에서 자동으로 제외된다.
    -->
    <update id="updateUnverifiedUser" parameterType="User">
        UPDATE users
           SET password_hash   = #{passwordHash}
             , nickname        = #{nickname}
             , birth_date      = #{birthDate}
             , phone           = #{phone}
             , gender          = #{gender}
             , address         = #{address}
             , agreed_terms    = #{agreedTerms}
             , agreed_privacy  = #{agreedPrivacy}
             , status          = #{status}
             , updated_at      = CURRENT_TIMESTAMP
         WHERE user_id = #{userId}
           AND email_verified = FALSE
           AND password_hash IS NOT NULL
    </update>

    <select id="selectUserByEmail" resultType="User">
        SELECT user_id
             , email
             , password_hash
             , nickname
             , birth_date
             , phone
             , gender
             , address
             , agreed_terms
             , agreed_privacy
             , role
             , status
             , email_verified
             , created_at
             , updated_at
          FROM users
         WHERE email = #{email}
    </select>

    <!-- refresh 시 최신 role 조회용 -->
    <select id="selectUserById" resultType="User">
        SELECT user_id
             , email
             , password_hash
             , nickname
             , birth_date
             , phone
             , gender
             , address
             , agreed_terms
             , agreed_privacy
             , role
             , status
             , email_verified
             , created_at
             , updated_at
          FROM users
         WHERE user_id = #{userId}
    </select>

    <!-- 재전송/인증 동시 요청을 직렬화하기 위한 행 잠금 조회. issueAndSend()/verify() 둘 다
         이걸 제일 먼저 호출해서 잠금 순서(users -> user_tokens)를 맞춤 -->
    <select id="selectUserByIdForUpdate" resultType="User">
        SELECT user_id
             , email
             , password_hash
             , nickname
             , birth_date
             , phone
             , gender
             , address
             , agreed_terms
             , agreed_privacy
             , role
             , status
             , email_verified
             , created_at
             , updated_at
          FROM users
         WHERE user_id = #{userId}
           FOR UPDATE
    </select>

    <!-- ===== 이메일 인증 코드 (user_tokens) ===== -->

    <insert id="insertUserToken"
            parameterType="UserToken"
            useGeneratedKeys="true"
            keyProperty="tokenId"
            keyColumn="token_id">
        INSERT INTO user_tokens (
               user_id
             , token_hash
             , purpose
             , expires_at
             , created_at
        ) VALUES (
               #{userId}
             , #{tokenHash}
             , #{purpose}
             , #{expiresAt}
             , #{createdAt}
        )
    </insert>

    <!-- 해당 유저의 아직 안 쓰인 최신 토큰 1건. verify()에서 검증 대상으로 씀 -->
    <select id="selectActiveUserToken" resultType="UserToken">
        SELECT token_id
             , user_id
             , token_hash
             , purpose
             , used_at
             , attempt_count
             , created_at
             , expires_at
          FROM user_tokens
         WHERE user_id = #{userId}
           AND purpose = #{purpose}
           AND used_at IS NULL
         ORDER BY created_at DESC
         LIMIT 1
    </select>


    <!-- 사용/폐기 여부와 상관없이 가장 최근 발급된 토큰 1건. 재발급 쿨다운 판단용 -->
    <select id="selectLatestToken" resultType="UserToken">
        SELECT token_id
             , user_id
             , token_hash
             , purpose
             , used_at
             , attempt_count
             , created_at
             , expires_at
          FROM user_tokens
         WHERE user_id = #{userId}
           AND purpose = #{purpose}
         ORDER BY created_at DESC
         LIMIT 1
    </select>

    <!-- 재전송 등으로 새 토큰을 발급하기 전, 기존에 살아있던 토큰을 무효화 -->
    <update id="invalidateActiveTokens">
        UPDATE user_tokens
           SET used_at = CURRENT_TIMESTAMP
         WHERE user_id = #{userId}
           AND purpose = #{purpose}
           AND used_at IS NULL
    </update>

    <!-- 검증 실패 시 시도 횟수 증가. maxAttempts 도달 시 같은 statement에서 토큰도 폐기 -->
    <update id="recordFailedAttempt">
        UPDATE user_tokens
           SET attempt_count = attempt_count + 1
             , used_at = IF(attempt_count + 1 >= #{maxAttempts}, CURRENT_TIMESTAMP, used_at)
         WHERE token_id = #{tokenId}
           AND used_at IS NULL
    </update>

    <update id="markUserTokenUsed">
        UPDATE user_tokens
           SET used_at = CURRENT_TIMESTAMP
         WHERE token_id = #{tokenId}
           AND used_at IS NULL
           AND expires_at > CURRENT_TIMESTAMP
    </update>

    <!-- 인증 완료 기록. users.email_verified + status 둘 다 여기서 같이 전환 -->
    <update id="markEmailVerified">
        UPDATE users
           SET email_verified = TRUE
             , status = 'ACTIVE'
         WHERE user_id = #{userId}
    </update>

    <!-- ===== 권한 관리 ===== -->

    <!-- 사업자 등록/삭제 시 role 부여·회수 (참가업체·부스 도메인이 UserRoleService를 통해 호출) -->
    <update id="updateUserRole">
        UPDATE users
           SET role = #{role}
             , updated_at = CURRENT_TIMESTAMP
         WHERE user_id = #{userId}
    </update>

    <!-- ===== 비밀번호 변경 ===== -->

    <!-- 로그인 상태에서 비밀번호 변경 -->
    <update id="updateUserPassword">
        UPDATE users
           SET password_hash = #{passwordHash}
             , updated_at = CURRENT_TIMESTAMP
         WHERE user_id = #{userId}
    </update>

    <!-- 비밀번호 분실 시 비밀번호 재설정 -->
    <select id="selectUserTokenByHash" resultType="UserToken">
        SELECT token_id
        , user_id
        , token_hash
        , purpose
        , used_at
        , attempt_count
        , created_at
        , expires_at
        FROM user_tokens
        WHERE token_hash = #{tokenHash}
        AND purpose = #{purpose}
        AND used_at IS NULL
    </select>

    <!-- ===== 관리자 계정 관리 ===== -->

    <select id="selectAdminAccounts" resultType="AdminAccountRow">
        SELECT u.user_id
             , u.email
             , u.nickname
             , u.status
             , f.fair_id
             , f.name                 AS fair_name
             , f.operation_start_date
             , f.operation_end_date
          FROM users u
    INNER JOIN fair_admin_assignments faa ON faa.admin_user_id = u.user_id
    INNER JOIN fairs f                    ON f.fair_id = faa.fair_id
         WHERE u.role = 'EVENT_ADMIN'
         ORDER BY faa.assigned_at DESC
    </select>

    <!-- 관리자 계정 정지/정지 해제 -->
    <update id="updateUserStatus">
        UPDATE users
           SET status     = #{status}
             , updated_at = CURRENT_TIMESTAMP
         WHERE user_id = #{userId}
    </update>

</mapper>

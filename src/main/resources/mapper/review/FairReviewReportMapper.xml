<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ms.petopia.api.review.mapper.FairReviewReportMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="reviewReportId" keyColumn="review_report_id">
        INSERT INTO fair_review_reports
            (review_id, reporter_user_id, reason, reason_detail, created_at)
        VALUES
            (#{reviewId}, #{reporterUserId}, #{reason}, #{reasonDetail}, #{createdAt})
    </insert>

    <select id="existsByReviewIdAndReporterUserId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
              FROM fair_review_reports
             WHERE review_id = #{reviewId}
               AND reporter_user_id = #{reporterUserId}
        )
    </select>

</mapper>

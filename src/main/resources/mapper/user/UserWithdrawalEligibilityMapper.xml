<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ms.petopia.api.user.mapper.UserWithdrawalEligibilityMapper">

    <!-- PENDING_PAYMENT/CONFIRMED만 진행 중 -->
    <select id="existsActiveReservation" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
              FROM reservations
             WHERE user_id = #{userId}
               AND status IN ('PENDING_PAYMENT', 'CONFIRMED')
        )
    </select>

    <!-- PENDING만 확인한다. COMPLETED는 이미 끝난 거래.
         FAILED/CANCELED/EXPIRED는 더 이상 유효하지 않다 -->
    <select id="existsPendingPayment" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
              FROM payment
             WHERE payer_user_id = #{userId}
               AND status = 'PENDING'
        )
    </select>

</mapper>

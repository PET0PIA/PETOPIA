<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ms.petopia.api.commisionrate.mapper.CommissionRateMapper">

    <sql id="columns">
        commission_rate_id, scope, rate, fair_id, updated_by_user_id, updated_at
    </sql>

    <select id="selectLatestGlobal" resultType="CommissionRateRow">
        SELECT <include refid="columns"/>
        FROM commission_rate
        WHERE scope = 'GLOBAL'
        ORDER BY updated_at DESC
        LIMIT 1
    </select>

    <select id="selectLatestByFair" resultType="CommissionRateRow">
        SELECT <include refid="columns"/>
        FROM commission_rate
        WHERE scope = 'FAIR'
        AND fair_id = #{fairId}
        ORDER BY updated_at DESC
        LIMIT 1
    </select>

    <insert id="insert" parameterType="CommissionRateRow"
            useGeneratedKeys="true" keyProperty="commissionRateId">
        INSERT INTO commission_rate (scope, rate, fair_id, updated_by_user_id, updated_at)
        VALUES (#{scope}, #{rate}, #{fairId}, #{updatedByUserId}, #{updatedAt})
    </insert>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ms.petopia.api.audit.mapper.AuditLogMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="auditId">
        INSERT INTO audit_log(
            user_id, actor_type, actor_role,
            action_type, target_type, target_id,
            before_value, after_value, occurred_at
        )VALUES(
            #{userId}, #{actorType}, #{actorRole},
            #{actionType}, #{targetType}, #{targetId},
            #{beforeValue}, #{afterValue}, #{occurredAt}
        )
    </insert>

    <select id="selectByTarget" resultType="AuditLogRow">
        SELECT audit_id, user_id, actor_type, actor_role,
            action_type, target_type, target_id,
            before_value, after_value, occurred_at
        FROM audit_log
        WHERE target_type = #{targetType}
            AND target_id = #{targetId}
        ORDER BY occurred_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectByActorUserId" resultType="AuditLogRow">
        SELECT audit_id, user_id, actor_type, actor_role,
            action_type, target_type, target_id,
            before_value, after_value, occurred_at
        FROM audit_log
        WHERE user_id = #{userId}
        ORDER BY occurred_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectByActionType" resultType="AuditLogRow">
        SELECT audit_id, user_id, actor_type, actor_role,
            action_type, target_type, target_id,
            before_value, after_value, occured_at
        FROM audit_log
        WHERE action_type = #{actionType}
        ORDER BY occurred_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByTarget" resultType="long">
        SELECT COUNT(*)
        FROM audit_log
        WHERE target_type = #{targetType}
            AND target_id = #{targetId}
    </select>

    <select id="countByActorUserId" resultType="long">
        SELECT COUNT(*)
        FROM audit_log
        WHERE user_id = #{userId}
    </select>
</mapper>
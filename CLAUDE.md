# 작업 방식

## 파일 수정 전 확인
코드/설정 파일을 수정(Edit/Write)하기 전에는 무엇을 어떻게 바꿀지 먼저 설명하고 사용자 승인을 받은 뒤에 적용한다. 여러 파일을 고쳐야 하는 작업이라도 한 번에 다 진행하지 않고, 파일 단위(또는 논리적 변경 단위)로 하나씩 확인받고 다음으로 넘어간다.

- 조사·탐색성 작업(Read/Grep/Glob/Bash 조회성 명령 등)은 확인 없이 바로 진행해도 된다. 대상은 실제로 파일 내용을 바꾸는 변경이다.
- 사용자가 "이번 건 한 번에 다 진행해줘"처럼 명시적으로 일괄 진행을 허용하면 그 요청을 우선한다.

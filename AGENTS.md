# PETOPIA 작업 규칙

## 변경 승인과 안전

- 사용자의 명시적 승인 없이는 어떤 파일도 수정하거나 삭제하지 않는다.
- 코드 변경이 필요하면, 변경 전에 대상 파일·변경 내용·예상 영향을 설명하고 사용자의 승인을 받는다.
- API 구현은 반드시 한 번에 하나의 엔드포인트만 진행한다. 다른 API를 함께 구현하거나 선행 구현하지 않는다.
- 읽기, 검색, 분석, 테스트만 자동으로 수행할 수 있다.
- `rm`, `git reset`, 파일 덮어쓰기, 데이터 삭제·변경 등 파괴적이거나 되돌리기 어려운 작업은 항상 사전 승인 후 수행한다.
- 승인되지 않은 변경을 위해 새 파일을 만들거나 기존 파일을 옮기거나 이름을 바꾸지 않는다.

## 담당 도메인 경계

- 담당 범위는 **예약/입장 도메인**으로 한정한다.
- 다른 도메인의 코드, 데이터 모델, API, 설정, 화면은 수정하지 않는다.
- 다른 도메인 변경이 필요하거나 그 가능성이 있으면, 다음을 먼저 사용자에게 설명하고 승인을 받는다.
  - 변경이 필요한 정확한 파일 또는 컴포넌트
  - 변경이 필요한 이유
  - 예약/입장 도메인 및 다른 도메인에 미치는 영향
  - 대안과 위험 요소

## 기획서와 설계 원칙

- 예약/입장 작업을 시작할 때는 먼저 `docs/internal/reservation-entry-context.md`를 읽는다. 이 문서는 해당 도메인의 압축 작업 컨텍스트다.
- 세부 정책, 화면 요구사항, 예외 처리 또는 요약에 없는 내용이 필요할 때만 `docs/internal/PETOPIA_통합기획서_v1.1.html`의 관련 부분을 확인한다. 원본 전체를 매번 읽지 않는다.
- 원본 기획서와 요약 문서는 내부 문서이며 GitHub에 푸시하지 않는다.
- 제공된 기획서는 최종 확정본이 아니며 언제든 변경될 수 있다. 기획서의 세부 사항을 확정된 정책이나 구현 요구사항으로 단정하지 않는다.
- 다만 기획서의 기본 목적과 핵심 사용자 흐름은 존중한다.
- 사용자가 새로운 정책·결정·기획서 변경을 명시적으로 확정하면, 같은 작업에서 `docs/internal/reservation-entry-context.md`를 함께 갱신한다. 이 요약 문서의 갱신은 해당 정책 확정에 수반되는 승인된 작업이다.
- 추정, 제안 또는 모호한 발언을 확정 정책으로 요약 문서에 기록하지 않는다. 확정 사항과 미정 사항을 구분해 기록한다.
- 테이블 구조는 기획서와 크게 달라질 수 있음을 전제로 한다. 스키마나 데이터 모델을 제안하기 전, 기존 구조와 정합성을 검토한다.
- 구조적 문제(확장성, 정합성, 트랜잭션, 권한, 상태 전이, 데이터 중복 등)가 보이면 구현 전에 설명하고 질문한다.
- 예약/입장 기능이 다른 도메인과 상호작용하는 경우, 의존성·데이터 소유권·API 계약·실패 시 동작을 함께 검토하고 사용자에게 확인한다.
- 정책이 미확정인 영역은 임의로 고정하지 않는다. 관리 가능한 CRUD 구조, 상태값, 설정 가능 항목을 우선 제안하고 사용자 확인을 거쳐 결정한다.
- 요구사항이 모호하거나 설계 선택이 필요한 경우, 구현을 시작하기 전에 반드시 사용자에게 질문한다.
